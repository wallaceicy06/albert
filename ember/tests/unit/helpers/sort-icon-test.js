import { sortIcon } from '../../../helpers/sort-icon';
import { module, test } from 'qunit';

module('Unit | Helper | sort icon');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = sortIcon(42);
  assert.ok(result);
});
